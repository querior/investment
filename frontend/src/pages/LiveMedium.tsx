import { Typography } from "antd";

export default function LiveMedium() {
	return (
		<div className="p-6">
			<Typography.Title level={3} className="!m-0">
				Live — Medium
			</Typography.Title>
		</div>
	);
}
